<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.knowledge.base.statistics.mapper.AiStatisticsMapper">

    <!-- 统计 AI 对话总数（本地投影表 stat_ai_conversation） -->
    <select id="countConversations" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM stat_ai_conversation
        WHERE deleted = 0
    </select>

    <!-- 统计 AI 用户提问总数（本地投影表 stat_ai_message） -->
    <select id="countUserMessages" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM stat_ai_message
        WHERE role = 'user'
          AND deleted = 0
    </select>

</mapper>
