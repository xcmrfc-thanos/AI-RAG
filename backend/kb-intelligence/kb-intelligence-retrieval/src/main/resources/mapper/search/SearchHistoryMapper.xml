<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.knowledge.base.search.mapper.SearchHistoryMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.knowledge.base.search.entity.SearchHistory">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="keyword" property="keyword" />
        <result column="search_count" property="searchCount" />
        <result column="created_at" property="createdAt" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, user_id, keyword, search_count, created_at
    </sql>

    <!-- 根据用户ID查询搜索历史列表 -->
    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM kb_search_history
        WHERE user_id = #{userId}
        ORDER BY search_count DESC, created_at DESC
    </select>

    <!-- 根据用户ID查询热门搜索关键词 -->
    <select id="selectHotKeywordsByUserId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM kb_search_history
        WHERE user_id = #{userId}
        ORDER BY search_count DESC
        LIMIT #{limit}
    </select>

    <select id="selectHotKeywordsByUserId" databaseId="oracle" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM kb_search_history
        WHERE user_id = #{userId}
        ORDER BY search_count DESC
        FETCH FIRST #{limit} ROWS ONLY
    </select>

    <!-- 根据用户ID和关键词查询 -->
    <select id="selectByUserIdAndKeyword" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM kb_search_history
        WHERE user_id = #{userId}
        AND keyword = #{keyword}
        LIMIT 1
    </select>

    <select id="selectByUserIdAndKeyword" databaseId="oracle" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM kb_search_history
        WHERE user_id = #{userId}
        AND keyword = #{keyword}
        FETCH FIRST 1 ROWS ONLY
    </select>

    <!-- 增加搜索次数 -->
    <update id="incrementSearchCount">
        UPDATE kb_search_history
        SET search_count = search_count + 1
        WHERE user_id = #{userId}
        AND keyword = #{keyword}
    </update>

    <!-- 更新或插入搜索历史（默认 MySQL；PostgreSQL 见下方 databaseId） -->
    <insert id="insertOrUpdate" parameterType="com.knowledge.base.search.entity.SearchHistory">
        INSERT INTO kb_search_history (user_id, keyword, search_count, created_at)
        VALUES (#{userId}, #{keyword}, 1, NOW())
        ON DUPLICATE KEY UPDATE
        search_count = search_count + 1,
        created_at = NOW()
    </insert>

    <!-- 更新或插入搜索历史（PostgreSQL，依赖 uk_user_keyword） -->
    <insert id="insertOrUpdate" databaseId="postgresql"
            parameterType="com.knowledge.base.search.entity.SearchHistory">
        INSERT INTO kb_search_history (user_id, keyword, search_count, created_at)
        VALUES (#{userId}, #{keyword}, 1, NOW())
        ON CONFLICT (user_id, keyword) DO UPDATE SET
        search_count = kb_search_history.search_count + 1,
        created_at = NOW()
    </insert>

    <!-- 更新或插入搜索历史（Oracle MERGE，依赖 uk_user_keyword） -->
    <insert id="insertOrUpdate" databaseId="oracle"
            parameterType="com.knowledge.base.search.entity.SearchHistory">
        MERGE INTO kb_search_history t
        USING (SELECT #{userId} AS user_id, #{keyword} AS keyword FROM dual) s
        ON (t.user_id = s.user_id AND t.keyword = s.keyword)
        WHEN MATCHED THEN UPDATE SET
            t.search_count = t.search_count + 1,
            t.created_at = SYSTIMESTAMP
        WHEN NOT MATCHED THEN INSERT (user_id, keyword, search_count, created_at)
            VALUES (s.user_id, s.keyword, 1, SYSTIMESTAMP)
    </insert>

    <!-- 删除搜索历史 -->
    <delete id="deleteByUserIdAndKeyword">
        DELETE FROM kb_search_history
        WHERE user_id = #{userId}
        AND keyword = #{keyword}
    </delete>

    <!-- 清空用户搜索历史 -->
    <delete id="clearByUserId">
        DELETE FROM kb_search_history
        WHERE user_id = #{userId}
    </delete>

    <!-- 统计用户的搜索历史数量 -->
    <select id="countByUserId" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM kb_search_history
        WHERE user_id = #{userId}
    </select>

    <!-- 根据关键词模糊搜索（默认 MySQL/PG CONCAT） -->
    <select id="searchByKeyword" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM kb_search_history
        WHERE user_id = #{userId}
        AND keyword LIKE CONCAT('%', #{keyword}, '%')
        ORDER BY search_count DESC, created_at DESC
    </select>

    <!-- Oracle：CONCAT 仅两参数，改用 || -->
    <select id="searchByKeyword" databaseId="oracle" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM kb_search_history
        WHERE user_id = #{userId}
        AND keyword LIKE '%' || #{keyword} || '%'
        ORDER BY search_count DESC, created_at DESC
    </select>

</mapper>
