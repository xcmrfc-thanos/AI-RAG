# AI-RAG 遗留治理计划（第 6 阶段）

> 制定日期：2026-07-11  
> 状态：**已完成**（2026-07-11，任务 29–41 全部完成）  
> 前置：优化路线图任务 **1–28 已全部完成**（见 [backend/docs/优化路线图.md](backend/docs/优化路线图.md)）  
> 关联：[readme_plan.md](readme_plan.md)、[backend/docs/系统功能解析.md](backend/docs/系统功能解析.md)

---

## 一、背景与边界

### 1.1 当前基线

| 维度 | 已完成摘要 |
|------|------------|
| 搜索域 | 双索引、collapse 分页、URL 单源、abort、组件化、高亮统一、0 结果引导 |
| 前端性能 | echarts/react-pdf 分包；FileManagement 主包 890KB→62KB；mammoth/xlsx 按需加载 |
| 工程化 | AdminCenter 收敛；Empty/Loading 已覆盖 14+ 页（含管理端、AI、通知等） |
| 后端验收 | 全仓 Java 21 `mvn test` 通过；MQ/RAG E2E 双索引验收；统计去 VIEW |
| 运维 | Intelligence JVM 512m/1g；搜索/RAG/图谱三池线程隔离 |
| 产品 | AI 三入口边界文档与页面文案统一 |

### 1.2 环境约束（继承）

- **无历史数据**，全新部署
- `stat_*` 投影表随 MQ 增量写入，**不做**历史数据迁移脚本
- JDK：**Java 21**（`D:\Users\environments\Java21`）
- Git：`frontend`、`backend` 分仓；`backend/docs/` 仅本地文档

### 1.3 本计划范围

仅覆盖**遗留治理**与**质量闭环**，不含新功能开发。  
原路线图「暂不推进」项（微服务拆分、换向量库、全站 UI 重做）**不纳入本计划**。

---

## 二、遗留问题全景

### 2.1 阻塞项（已解决）

| ID | 问题 | 现状 | 影响 |
|----|------|------|------|
| B1 | `kb-intelligence-llm` 编译失败 | ✅ 任务 29 已修复 `ElasticsearchClient` 依赖 | — |
| B2 | 全链路测试未跑通 | ✅ 任务 30 全仓 15 模块 BUILD SUCCESS | — |

### 2.2 架构债务（已解决或已文档化）

| ID | 问题 | 现状 | 目标 |
|----|------|------|------|
| A1 | 统计跨库 VIEW 历史债务 | ✅ 任务 32–34 去 VIEW + 投影补全 | 已达成 |
| A2 | MQ/RAG 验收深度不足 | ✅ 任务 31 E2E + 任务 19 监听器 | 已达成 |
| A3 | Intelligence 单 JVM 多职责 | ✅ 任务 36 三池隔离；拆进程仍为可选 | 短期目标已达成 |

### 2.3 性能 / 运维（已解决）

| ID | 问题 | 现状 | 目标 |
|----|------|------|------|
| P1 | Intelligence JVM 偏小 | ✅ 512m/1g（任务 35） | 已达成 |
| P2 | mammoth 独立 chunk 偏大 | ~497KB，已懒加载 | 接受现状（低优先级） |

### 2.4 前端工程化（本阶段已解决项）

| ID | 问题 | 涉及页面 | 状态 |
|----|------|----------|------|
| F1 | Empty/Loading 第三批 | admin/*、AI 页、通知中心等 | ✅ 任务 37 |
| F2 | 知识图谱空态未统一 | `KnowledgeGraphPage` | ✅ 任务 42（可选收尾） |
| F3 | 共享样式分散 | document-card 等 | ✅ 任务 38 |
| F4 | Admin 无统一侧栏壳 | 各 `/admin/*` | ✅ 任务 44（可选收尾） |
| F5 | AdminCenter 卡片统计硬编码 | 模块卡片 | ✅ 任务 39 |

### 2.5 产品与文档（已解决）

| ID | 问题 | 说明 | 状态 |
|----|------|------|------|
| D1 | AI 写作 / 助手 / 搜索边界 | 见 `docs/ai-entry-boundaries.md` | ✅ 任务 40 |
| D2 | 路线图文档头信息过期 | `优化路线图.md` 状态同步 | ✅ 任务 41 |

### 2.6 杂项

| ID | 问题 |
|----|------|
| M1 | `frontend/tsconfig.json` 本地改动未提交（与任务无关，勿误提交） |

---

## 三、新实施计划（任务 29 起）

### 第 6 周 — 质量闭环（优先）

> **目标**：恢复全仓编译与测试能力，补齐 RAG 验收深度。

| 序号 | 任务 | 维度 | 优先级 | 关键动作 | 验收标准 |
|------|------|------|--------|----------|----------|
| **29** | 修复 llm 模块 `ElasticsearchClient` 编译 | 后端 | **P0** | 补全 import/依赖或 Bean 配置；`mvn compile -pl kb-intelligence-llm` 通过 | ✅ 该模块单独编译无错误 |
| **30** | Java 21 全仓 `mvn test` 验证 | 后端 | **P0** | `JAVA_HOME=Java21` 下根目录 `mvn test`；记录失败模块与原因 | ✅ 全仓 15 模块 BUILD SUCCESS，14 用例全绿 |
| **31** | MQ/RAG E2E 验收（深化任务 19） | 架构 | **P1** | ✅ 发布文档集成测试：断言 `kb_document` + `kb_chunk` 可搜、关键词/chunk 命中 | `@SpringBootTest` + 内存 ES Mock 用例绿灯 |

### 第 7 周 — 统计架构治理

> **目标**：统计服务只读本库，去掉跨库 VIEW 运行时依赖。

| 序号 | 任务 | 维度 | 优先级 | 关键动作 | 验收标准 |
|------|------|------|--------|----------|----------|
| **32** | 统计查询审计 | 架构 | **P1** | ✅ 扫描 Mapper/SQL，产出审计表 `sql/schema/statistics-sql-audit.md` | 文件→SQL→替代方案完整；运行时无跨库 VIEW 引用 |
| **33** | 统计宽表去 VIEW 改造 | 架构 | **P1** | ✅ 概览改 stat_* / 预聚合；废弃 VIEW 脚本；移除 kb_view_history 回退 | 运行时无 CREATE VIEW 依赖；StatisticsServiceImpl 无跨库回退 |
| **34** | 统计投影补全（若有缺口） | 架构 | **P2** | ✅ 补用户删/更、审核流文档投影；覆盖清单 `statistics-projection-coverage.md` | 审计表所列 stat_* 查询均有本库写路径 |

### 第 8 周 — 运维与 Intelligence 调优

| 序号 | 任务 | 维度 | 优先级 | 关键动作 | 验收标准 |
|------|------|------|--------|----------|----------|
| **35** | Intelligence JVM 内存调优 | 性能 | **P2** | ✅ 调整 deploy/启动脚本 `-Xms/-Xmx`；压测搜索+RAG 场景 | 文档记录推荐值；无 OOM |
| **36** | Intelligence 线程池隔离 | 架构 | **P2** | ✅ 搜索 / RAG / 图谱构建分池配置 | 高负载下互不饿死；有配置说明 |

### 第 9 周 — 前端工程化收尾

| 序号 | 任务 | 维度 | 优先级 | 关键动作 | 验收标准 |
|------|------|------|--------|----------|----------|
| **37** | Empty/Loading 第三批 | 工程化 | **P2** | ✅ `admin/SettingsPage`、`StatisticsPage`、`PermissionsPage`、`AIAssistantPage`、`AIWritingPage`、`NotificationCenterPage` | 页面级加载/空态统一组件 |
| **38** | 共享样式抽取（document-card） | UI | **P2** | ✅ 抽 1–2 个高频卡片/列表样式到 `components/common` | 至少 2 页复用，无视觉回归 |
| **39** | AdminCenter 数据接 API（可选） | UI | **P3** | ✅ 模块卡片统计值改 `statisticsService` | 无硬编码「12」「1.2K」类展示 |

### 第 10 周 — 产品定义与文档

| 序号 | 任务 | 维度 | 优先级 | 关键动作 | 验收标准 |
|------|------|------|--------|----------|----------|
| **40** | AI 三入口产品边界文档 | 产品 | **P2** | ✅ 在 `docs/` 写清搜索/助手/写作职责与入口文案 | 三页面副标题/引导与文档一致 |
| **41** | 路线图与计划文档收口 | 文档 | **P2** | ✅ 更新 `优化路线图.md` 状态；本计划勾选完成项 | 文档与代码状态一致 |

---

## 四、建议执行顺序（总览）

```text
阻塞修复（29→30→31） → 统计去 VIEW（32→33✅）
        ↓
运维调优（35→36） → 前端收尾（37→38→39） → 产品/文档（40→41✅）
```

**阶段状态**：遗留治理阶段 **已全部完成**（2026-07-11，含可选收尾 42–45）。后续按需执行 `deploy/scripts/verify-all.ps1` 联调冒烟或启动新功能规划。

---

## 五、验收标准（阶段完成）

### 第 6 周完成标志

- [x] `mvn compile` 全模块通过（Java 21）
- [x] `mvn test` 全模块通过，或仅剩环境类失败且有文档说明
- [x] RAG E2E 用例覆盖「发布 → 双索引可搜」

### 第 7 周完成标志

- [x] `kb-statistics` 运行时 SQL 不依赖跨库 VIEW
- [x] `sql/schema/kb_statistics.sql` 为唯一权威 schema（旧 export VIEW 已标注废弃）

### 第 8 周完成标志

- [x] Intelligence JVM 调优文档与启动脚本分服务配置
- [x] 搜索 / RAG / 图谱三池线程隔离

### 第 9 周完成标志

- [x] 主要用户路径页面（搜索、文档、收藏、草稿、管理入口）Empty/Loading 统一
- [x] `npm run build` 无新增错误

### 第 10 周完成标志

- [x] AI 三入口产品边界文档与页面文案一致
- [x] `优化路线图.md` 与 `遗留治理计划.md` 状态同步

---

## 六、暂不推进（明确排除）

- 微服务进一步拆分（4 BC 规模足够）
- 更换向量库（ES 混合检索已可用）
- 全站 UI 重做
- 历史数据迁移脚本
- Admin 统一侧栏大改（~~工作量大，降为 P3 可选~~ → ✅ 任务 44 已完成）

---

## 七、参考文件索引

| 主题 | 路径 |
|------|------|
| 已完成任务记录 | `readme_plan.md` |
| 原优化路线图 | `backend/docs/优化路线图.md` |
| 统计 schema | `backend/sql/schema/kb_statistics.sql` |
| Intelligence JVM 调优 | `backend/sql/schema/intelligence-jvm-tuning.md`、`deploy/start-services.ps1` |
| Intelligence 线程池隔离 | `backend/sql/schema/intelligence-executor-isolation.md`、`IntelligenceExecutorConfig.java` |
| 统计投影监听 | `backend/kb-statistics/.../CoreStatisticsProjectionListener.java` |
| llm 编译问题 | `backend/kb-intelligence/kb-intelligence-llm/.../ElasticsearchVectorIndexServiceImpl.java` |
| 共享 Empty/Loading | `frontend/src/components/common/EmptyState.tsx`、`PageLoading.tsx` |
| AI 三入口边界 | `docs/ai-entry-boundaries.md`、`frontend/src/constants/ai-entry.ts` |
| 共享文档卡片 | `frontend/src/components/common/DocumentCard.tsx`、`DocumentListCard.tsx` |
| Admin 导航 | `frontend/src/constants/admin-nav.ts`、`AdminLayout.tsx` |
| 联调冒烟 | `deploy/scripts/verify-integration.ps1`、`verify-all.ps1` |

**可选后续**：`deploy/scripts/verify-all.ps1` 一键联调冒烟。

---

## 八、可选收尾（任务 42–45）

| 序号 | 任务 | 维度 | 状态 | 说明 |
|------|------|------|------|------|
| **42** | 知识图谱空态统一（F2） | 工程化 | ✅ | `KnowledgeGraphPage` 接入共享 `EmptyState` |
| **43** | stat_role/stat_team 投影补全 | 架构 | ✅ | Admin 概览 totalRoles/totalTeams |
| **44** | Admin 统一侧栏壳（F4） | UI | ✅ | `AdminLayout` + 嵌套路由 |
| **45** | AdminPageHeader + 联调冒烟 | 工程化 | ✅ | 7 子页标题统一 + verify-integration |
| **46** | 联调验收自动化自检 | 运维 | ✅ | mvn test + build + verify-integration.ps1 |
| **47** | 运行态微服务启动修复 | 运维 | ✅ | start-services.ps1 修复 + 5 服务 UP |

---

## 九、变更记录

| 日期 | 说明 |
|------|------|
| 2026-07-11 | 任务 47 完成：start-services.ps1 修复，5 微服务启动成功 |
| 2026-07-11 | 任务 46 完成：联调自动化自检 + verify-integration.ps1 + 验收记录 |
| 2026-07-11 | 任务 45 完成：AdminPageHeader + verify-integration 联调冒烟 |
| 2026-07-11 | 任务 44 完成（可选）：AdminLayout 统一侧栏 + admin 嵌套路由 |
| 2026-07-11 | 任务 43 完成（可选）：stat_role/stat_team 投影补全 |
| 2026-07-11 | 任务 42 完成（可选）：KnowledgeGraphPage 空态接入 EmptyState |
| 2026-07-11 | **阶段完成**：任务 41 收口；任务 29–41 全部完成 |
| 2026-07-11 | 任务 40 完成：AI 三入口产品边界文档与页面文案统一 |
| 2026-07-11 | 任务 39 完成：AdminCenter 模块卡片接 statisticsService，移除硬编码统计 |
| 2026-07-11 | 任务 38 完成：DocumentCard/DocumentListCard 共享组件，Dashboard 与最近访问页复用 |
| 2026-07-11 | 任务 34 完成：统计投影补全（用户删/更、审核流文档投影） |
| 2026-07-11 | 任务 33 完成：统计去 VIEW（废弃脚本、概览改 stat_*、移除回退） |
| 2026-07-11 | 任务 32 完成：统计 SQL 审计报告 statistics-sql-audit.md |
| 2026-07-11 | 任务 31 完成：MQ/RAG E2E 双索引验收（@SpringBootTest + 内存 ES Mock） |
| 2026-07-11 | 任务 30 完成：Java 21 全仓 mvn test 15 用例全绿 |
| 2026-07-11 | 任务 29 完成：llm 模块 ElasticsearchClient 编译修复 |
| 2026-07-11 | 初版：任务 1–28 完成后，遗留问题重新编排为任务 29–41 |
